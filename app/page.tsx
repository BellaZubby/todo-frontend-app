import Image from "next/image";

const Home = () => {
  return (
    <section aria-labelledby="title">
      <h2 id="title" className="sr-only">Task dashboard</h2>
      <p className="text-sm text-gray-600 dark:text-gray-400">
        We will add the task dashboard here shortly.
      </p>
    </section>
  );
}

export default Home
